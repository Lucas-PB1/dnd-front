---
description: Testes — Vitest e Cypress
globs: tests/**,**/*.test.ts,**/*.spec.ts,cypress/**
alwaysApply: false
---

# Testes

- **Unitário**: Vitest — `tests/entities/`, `tests/features/`, `tests/shared/`
- **E2E**: Cypress em `cypress/`
- Rodar: `pnpm test:run` · `pnpm test:e2e`

Skills: `testing-vitest` · `testing-cypress`
