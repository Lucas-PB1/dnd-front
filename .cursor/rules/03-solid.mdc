---
description: Princípios SOLID adaptados ao FSD
alwaysApply: true
---

# SOLID (FSD)

| Princípio | No dnd-front                                      |
| --------- | ------------------------------------------------- |
| **SRP**   | Um slice = uma responsabilidade de produto        |
| **OCP**   | Estender via novo slice, não editar `shared`      |
| **LSP**   | Tipos em `entities/` estáveis para features       |
| **ISP**   | `index.ts` exporta só API pública do slice        |
| **DIP**   | Features usam `shared/api/` — não fetch inline    |

`features/*/model/` não importa `@supabase/supabase-js` direto — usar `shared/api/supabase`.

Skill: `solid`
